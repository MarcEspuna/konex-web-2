# Konex Web (Implementation)

This is the Next.js implementation scaffold, placed under `codex/site` per project instructions.

## Local dev
1. `npm install`
2. `npm run dev`

## Notes
- Locale routes are `/es` and `/en`.
- Payments are stubbed (Stripe and PayPal) and can be wired later.
